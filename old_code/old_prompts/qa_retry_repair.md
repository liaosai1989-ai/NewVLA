上次输出无法解析为合法结果。请严格修正，并重新输出。

必须满足：

- 只输出一个 JSON 数组。
- 不要任何解释、前后缀或 Markdown 代码围栏。
- 第一个非空白字符必须是 `[`
- 数组元素必须是对象。
- 每个对象必须且只能包含两个键：
  - "question"
  - "answer"
- 键顺序必须固定为先 "question"，后 "answer"
- 两个字段的值都必须是字符串。
- 不要输出额外字段。
- 不要输出思考过程。
- 不要输出标签、前言、总结或备注。
- 若正文没有足够语义内容可生成问答，只输出空数组 `[]`。
- 若调用方已指定输出语言，请统一使用该语言，不要混用。

请直接输出修正后的 JSON 数组。
