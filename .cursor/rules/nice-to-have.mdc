---
description: 待优化点登记：用户明文要求写入 NiceToHave.md；plan 落地后回写状态与 spec/plan 索引；编辑前先读该文件内的填写规则
alwaysApply: true
---

# NiceToHave.md 维护

仓库根目录 `NiceToHave.md` 用于登记**待优化功能点**及其实现追踪。

## 何时新增条目

- 仅在用户**明文要求**将某待优化点记入清单时，才向 `NiceToHave.md` 新增或扩充记录。
- 不要自行把「觉得可以改进」的点写进去，除非用户明确说要登记。

## 编辑前必读

- 打开 `NiceToHave.md` 后，先阅读文内 **「填写规则」**（含总表与正文、ID、优先级、状态、`spec`/`plan` 索引等约定），再动笔。

## plan 落地后

- 每次 **plan 已落地**（实现与 spec/plan 一致）时，同步更新 `NiceToHave.md`：
  - 在 **待优化点总表** 中更新对应行的 **状态**、**spec 索引**、**plan 索引**（无则填 `-`，与文件内规则一致）。
  - 在 **正文记录** 中补充简要变更说明；已实现条目**不删除**，状态改为 **已实现**。

## 与 Trae 约定

- `.trae/rules/tbd.md` 与本规则语义一致时，以本仓库 **`NiceToHave.md` 内现行填写规则** 为操作细则。
