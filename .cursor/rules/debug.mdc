---
description: 调试会话：解决优先于成因解说；根因+证据为修复服务；预期不符视为缺陷；自行深挖后汇报，不甩排查清单
alwaysApply: false
---
# 调试会话（Debug）

用户处于排障、复现异常或追问「为什么不符预期」时，按本节执行。与 `agentaction.mdc` 中「反代码自洽」一致处不重复展开。

## 1. 少辩护，多定位

- **不要**用长篇说明「代码逻辑自洽」「按设计就是这样」来回避问题。
- **要**用可验证证据（调用链、状态、日志、测试、最小复现）支撑判断；**成因说明只为修复或验收服务**，不写「机制综述」占满篇幅。
- **反面模式（须避免）**：连续多轮大段解释「为什么现在这样」「历史上怎样设计的」，却无**已落地修改**、无可执行下一步或明确阻塞项——用户要的是问题解决，不是现象学论文。
- **反面案例（供对照，勿重复其模式）**：Cursor 会话 `7b0db0ef-2917-45f1-9244-950f610bad08`（成因说明堆叠、落地修复滞后）。

## 2. 预期不符即问题

- 行为与需求、验收标准或用户明确预期不一致时，**先当 BUG / 缺口处理**，再区分是实现错误、规格遗漏还是沟通偏差。
- **不要**用「模块设计上就是这样」单方面结案；若属设计取舍，须明确写出取舍与用户目标的 gap。

## 3. 查到根因再汇报 — 且交付物是「解决」，不是解说

- **不要**轮流抛出「下一步可查 A、再查 B」让用户驱动排查；**代理应在权限内自行查到根因或查到当前环境可确定的最深一层**，再一次性汇报。
- **汇报优先顺序**：**修复或明确动作（含补丁/命令/配置变更）** → 根因摘要（少则一段，够用即可）→ 证据锚点 → 若不能做则 **blocked 点**。能改代码/配置则**先改再汇报**，不要先写三千字成因分析再动手。
- 仅在确需用户提供密钥、访问权限、业务决策或物理环境信息时，列出**阻塞项**，而非泛泛的排查清单。

## 自检

删除所有「代码本身没问题」类辩护后，回答仍包含：**可核对的事实**与**对用户问题的直接回应**，方向才对。

再检：若删掉所有「为什么是这种表现」段落，答复里是否仍留有**可执行的解决或明确阻塞**？若否，重写为解优先。
