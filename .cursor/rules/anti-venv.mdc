---
description: 维护仓可用 venv 调试；生产/执行工作区禁止以 venv 为线上运行时、禁止生成物落 venv 树
alwaysApply: true
---

# 反 VENV（生产禁止 + 生成物落盘）

## 仓库类型

| 场景 | venv |
|------|------|
| **维护仓库**（管线源码、bootstrap、模板所在 clone） | **允许**：激活 `.venv`、用其解释器跑 `pip`/`pytest`/本地脚本。**禁止**把 **Agent/调度写入的任务产出** 放进 venv 树（见下节）。 |
| **生产仓库 / 执行工作区**（物化后的 `VLA_WORKSPACE_ROOT`、线上跑管线的那份） | **禁止 VENV（运行时）**：不得把工作区内 `.venv`/`venv`（及专用于 venv 的同名隔离目录）作为 **线上 Webhook、RQ、worker、编排入口** 的 **正式/唯一** Python 依赖载体；生产交付用 **容器内解释器**、**镜像固定路径**、或平台约定运行时，**禁止**把「必须 `Activate` 仓内 venv 再启动」写进正式运维 SOP。**禁止（落盘）**：任何 **新生成 / 物化 / 任务产出** 不得种在 venv 树或 `site-packages` 下，一律走合约路径（如 `.cursor_task/…`）。 |

## 生产侧硬性要求（运行时）

- **目标态**：线上进程 **不**依赖工作区 tree 里自建 venv；与 `runtime/webhook` 等目录是否曾用 `pip install` 装包 **解耦**：装机/对齐可以本机 venv，**正式跑法**须与「无仓内 venv」或可替换为镜像运行时一致。
- **bootstrap 物化**：若物化结果里仍带 `runtime/.../.venv`，仅视为 **装机或过渡**；**不得**将其当作生产唯一真源；收敛后应以平台运行时为准。
- **与维护仓区别**：维护 clone 里子包 `.venv` 仅服务 **开发与 CI**，**不**映射为执行工作区生产规范。

## 硬性要求（生成物目标路径）

- **新目录、新文件**（代码生成、脚手架、**任务产出**、webhook 物化等）**不得**以 **生产 / 执行工作区** 内 **Python 虚拟环境目录或其子树** 为落点。
- 常见 **禁止**作为 **生成根** 的形态：
  - `.venv/`、`venv/`、`ENV/`、专用于 venv 的 `env/`
  - `**/site-packages/**`、`**/Lib/site-packages/**`
- **维护仓库**内：**Agent/调度** 写入的生成目录同上禁止进 venv；**例外**仅用户 **明确要求** touch venv 内某文件、或文档写明必须改 venv 时。

## 与任务目录原则配合

- 任务注入与产出优先 **`.cursor_task/{run_id}/`**（见 `workplacestructure`），**不是** `某子包/.venv/`。

## 自检

- **生产 / 执行侧**：启停/编排是否仍 **硬绑定** 仓内 `Activate`+`.venv` → 改为平台约定运行时；落盘路径若解析进 `.venv`/`site-packages` → 改合约目录。
- **维护仓**：测试/脚本用 venv **可以**；**写盘前**再确认未误闯 venv。
