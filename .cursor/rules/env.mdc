---
description: 维护仓根 .env 与本仓库开发真源；执行工作区根 .env 为运行合同真源；old_code 仅参考；显式引用
alwaysApply: true
---

# 环境与敏感信息

## 两份 `.env`（勿混）

| 位置 | 用途 |
|------|------|
| **本仓库根目录** `{CLONE_ROOT}/.env`（即当前维护仓 **项目根** 下的 `.env`） | **维护 / 开发 / 排障** 真源：在本仓库内读写的密钥与合同**以此为准**；可作为 **bootstrap** 初始化时复制到执行工作区的 **种子**（见 `docs/superpowers/specs/2026-04-28-production-bootstrap-deployment-design.md` §2.1）。 |
| **执行工作区根** `{WORKSPACE_ROOT}/.env` | **管线运行** 合同唯一真源：`cursor agent` cwd 同层；生产起 webhook/RQ 时设 `VLA_WORKSPACE_ROOT` 指向工作区后加载**此文件**。**不是**本维护仓根目录下的那份；由 `materialize-workspace` 从维护仓种子生成首份，其后以工作区台账为准。 |

在本 **维护仓库** 里改代码、跑本仓工具时：**默认**仍以 **本仓库根** `.env` 为加载目标（除非某命令显式指向工作区 `.env`）。

## 通用规则

- 项目内的 `.env` 可直接读取；代码中须 **显式** 引用（环境变量、配置层、参数），勿隐式依赖未声明来源。
- `old_code/` 下旧实现用的 `.env`，**仅供对照**，当前管线**不得**依赖其中值。
- 允许在对话中明文沟通秘钥、key、token 等（按本工作区约定）。
