---
description: 根目录 .env 为唯一真源；old_code 仅参考；显式引用；允许对话明文讨论秘钥等
alwaysApply: true
---

# 环境与敏感信息

- 本项目的 **唯一真源** `.env` 位于**项目根目录**；实现与运行所需的密钥/环境变量**以此为准**。
- 项目内的该 `.env` 可以直接读取；在代码中仍应通过显式方式引用（如环境变量、配置层），不要隐式依赖未声明的来源。
- `old_code/` 目录下为旧实现使用的 `.env`，**仅供对照参考**，当前管线与实现**不得**依赖或使用其中的值。
- 允许在对话中明文沟通秘钥、key、token 等敏感信息（按本工作区约定）。
