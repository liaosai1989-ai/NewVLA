---
description: plan 落地时同步更新 prompts/ 下 AGENTS 与 rules 模板，面向管线执行工作区而非本维护仓
alwaysApply: true
---

# plan 落地时更新 `prompts/`

## 读者是谁（必须先对齐）

- `prompts/AGENTS.txt` 与 `prompts/rules/**` 是 **未来实际跑管线任务的「管线工作区」** 里的工作手册与执行指引：拷入该工作区后，由其中的 Agent 按约束完成同步、上传等任务。
- 它们 **不是** 本维护仓库的自用说明；维护仓的根 `AGENTS.md`、plan/spec、源码注释等才是「造系统、维护系统」一侧的文档。
- renew 时一切表述默认读者 = **在该工作区内干活的 Agent / 操作者**，语言与结构要便于按步骤执行与自检，而不是复述本仓内部实现细节。

## 何时 renew

在 **本仓库** 中，某份 **plan 已落地**（对应实现完成且与 design/spec 一致）时，**必须** 审视并 **renew（同步更新）** `prompts/` 目录下的**长期注入模板**：

- `prompts/AGENTS.txt`：分发到管线工作区后写入的 **`AGENTS.md` 的唯一模板**，只服务 **执行侧**（如何在该工作区内读上下文、用工具、交产物）。根目录 **`AGENTS.md` 是另一份文档**：只说明本维护仓库是什么、边界在哪，**与 `prompts/AGENTS.txt` 不是同一类东西**，禁止互拷、禁止要求二者正文「对齐」。renew 时只改执行手册；初始化代码只需按约定把 `prompts/AGENTS.txt` 落到目标工作区，不必与根 `AGENTS.md` 保持内容一致。
- `prompts/rules/**`：目标工作区 `rules` 的模板（含各子路径，如 `qa` 等）。内容须与当前管线约束、多 folder 映射、输出契约等 **对执行者可见、可遵守** 的规则一致。

## 回写怎么写（禁止机械搬运）

- 从 **管线工作区要遵守什么、先读什么、后做什么、交付什么** 出发改写；把落地实现里 **影响行为与边界** 的结论沉淀为可执行条款。
- **不要** 把 plan 的章节标题、任务列表、changelog 式「我们实现了某某模块」原样贴进 `prompts/`；维护仓的实现过程与文件路径除非执行者必须知道，否则不写入或仅保留最小必要引用（例如「先读 `task_context.json`」这类运行时契约）。
- 若某次落地只改本仓内部、对执行工作区的行为与规则无影响，则对 `prompts/` **最小核对即可**，无变更不动文件。

## 与文档回写的关系

- 与 `docedit.mdc` 中「落地后回写 plan/spec 状态」**并列**：文档记 **落地状态与设计/实现索引**，**`prompts/` 记可分发、给执行工作区用的模板**；二者都应在落地阶段完成，避免只改代码、模板仍陈旧。
- 若 plan 只改文档、与 Agent 行为或工作区注入无关，可仅作最小核对（无变更则不动文件）。

## 边界

- **`task_prompt.md`** 等由 webhook 侧注入的运行时物 **不在** `prompts/` 中作为固定模板资产维护（参见根目录 `AGENTS.md` 约定），勿误并入 `prompts/` 的「renew」范围，除非产品明确改为由本仓版本化该模板。

## 自检

落地合并前可自问：若此刻用本仓初始化一个 **新的管线执行工作区**，拷过去的 `AGENTS` / `rules` 是否让其中的 Agent **能独立完成一次任务**（读哪些上下文、遵守哪些约束、产出落哪里），且与当前真实行为一致？若读起来像本仓开发笔记或 plan 摘录，则先改成执行者手册再合并。
